[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "picostream"
version = "1.0.0"
dependencies = [
    "numpy",
    "loguru",
    "picosdk",
    "h5py",
    "pyqtgraph",
    "PyQt5",
    "numba",
    "click",
]

[project.scripts]
picostream = "main:main"
